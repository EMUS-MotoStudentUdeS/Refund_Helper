import{describe,expect,it}from'vitest';import{formatCurrency,formatDate,parseAmount,sanitizeFilename}from'./formatUtils';describe('formats',()=>{it('accepte virgule et point',()=>{expect(parseAmount('12,50')).toBe(12.5);expect(parseAmount('12.50')).toBe(12.5)});it('formate la monnaie',()=>expect(formatCurrency('12.5')).toBe('12,50'));it('formate la date',()=>expect(formatDate('2026-07-20')).toBe('20/07/2026'));it('nettoie le nom',()=>expect(sanitizeFilename('ÉMUS / test:*')).toBe('EMUS_test'))});
