import type{ValidationErrors}from'../types/reimbursement';
export function ValidationSummary({errors}:{errors:ValidationErrors}){const list=[...new Set(Object.values(errors).filter(Boolean))];return list.length?<div className="warning-summary" role="status"><strong>Points à vérifier avant de continuer :</strong><ul>{list.map(x=><li key={x}>{x}</li>)}</ul><p>Ces avertissements peuvent être ignorés si le formulaire doit rester partiellement rempli.</p></div>:null}
